# Runtime / per-instance state (excluded by the import script, but defensive)
.openclaw/
memory/
*.session
*.jsonl
.DS_Store

# Per-device skill venvs (created on first use)
.venv/
claude-code-skills/*/.venv/
__pycache__/
*.pyc
